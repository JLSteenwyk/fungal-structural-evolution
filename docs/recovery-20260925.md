# Recovery after the September 25 host reboot

The host had approximately eight minutes uptime at the first recovery check.
All four previously monitored project process handles were missing, and no
fungal transient user services remained loaded. This was confirmed termination,
not an observation timeout. An unrelated IQ-TREE process was left untouched.

Three CPU services were restored with new boot-bound recovery plans, preserving
the original plans and outputs:

- Species PMSF: `metadata/recovery_20260925_pmsf_plan.json`, 16 CPU equivalents,
  650 GiB memory limit. The checkpoint was hashed and copied before recovery;
  the native log confirms restoration of site frequencies, the initial tree,
  and 1,000 bootstrap trees. This does not mean inference is complete.
- ESMFold-based paired fits: `metadata/recovery_20260925_paired_fit_plan.json`,
  four CPU equivalents, 16 GiB memory limit; 228 existing fit receipts.
- AlphaFold-based paired fits:
  `metadata/recovery_20260925_afdb_paired_fit_plan.json`, four CPU equivalents,
  16 GiB memory limit; 120 existing fit receipts.

Commands, process identities, and boot ID are recorded in
`metadata/recovery_20260925_core_launches.json`. All three services were verified
live with native child processes. Original dependency pins are checked by the
recovery wrappers. Runtime remaining is uncalibrated. No paid resources or GPU
predictions were started.

The geometry log contains 101 processed markers, but inspection of
`prepare_paired_site_geometry.py` showed that results were accumulated in memory
until the entire cohort finished. The final output directory does not exist.
Those log entries therefore do not constitute recoverable geometry outputs.
Geometry requires checkpointing before recomputation. Its dependent controllers,
the other queued analyses, and retrieval still require recovery review; they
must not be described as running based on their pre-reboot state files.

## Geometry recovery

A separate `prepare_checkpointed_paired_site_geometry.py` preserves the original
geometry calculations and output format while saving each completed marker.
Checkpoint writes use a temporary file, fsync, atomic rename, and directory
fsync. An exclusive lock prevents concurrent writers. Resume checks source
receipt hashes, implementation hashes, marker identities, pair counts, and
serialized payload hashes before reusing marker results. Incomplete temporary
writes are not used. The original producer remains unchanged so previous
provenance pins remain valid.

`check_geometry_checkpoint_recovery.py` compared original and checkpointed
outputs for two real taxa from one marker, then repeated the new calculation
from its checkpoint and deliberately corrupted the payload hash. Both TSV
comparisons were byte-identical, and corruption was rejected. Only cohort
receipt validation was mocked to limit this test; coordinate and PAE hash checks
were executed. This is a recovery regression test, not a full numerical audit.

The complete 122-marker, 2,527,033-pair calculation was relaunched with one CPU
equivalent, 64 GiB memory, no swap, and no GPU through
`scripts/recover_completed_geometry.py`. It reuses the completed PAE union and
will run the original full grid/character audit after geometry finishes. The
recovery plan and launch identity are
`metadata/recovery_20260925_geometry_plan.json` and
`metadata/recovery_20260925_geometry_launch.json`. Checkpoints reside in
`results/structural_comparisons/paired-site-esmfold-checkpoints-20260925-v1/`.
Prior in-memory progress cannot be recovered and must be recomputed.
Dependent-controller recovery remains outstanding.

## Site-rate and exposure controller recovery

The site-rate export and site parsimony/exposure controllers have been restored
with the recovered paired-fit producer's exact PID, creation time, and command.
Both passed their existing `--check-config` checks, including all dependency
pins. All six stage-output directories were absent before launch. The original
stage commands, output locations, prerequisite completion checks, and resource
limits are unchanged; fresh controller directories preserve the prior records.

Plans are `metadata/recovery_20260925_site_rate_exports_plan.json` and
`metadata/recovery_20260925_site_exposure_plan.json`; commands and process
identities are in `metadata/recovery_20260925_site_controller_launches.json`.
Both live services logged that they are waiting for producer 75123. They will
validate the completed fit/audit receipts before beginning their stages.
These are queued analyses, not completed exports or exposure results.
The downstream rate-comparison/coupling controllers, geometry-tree benchmark
and readbacks, resampling, fourth PMSF analysis, and retrieval still need review.

## Rate-comparison and coupling controller recovery

The rate-comparison controller now waits on the recovered rate-export and
exposure services. Its prerequisite paths point to the new controller receipts;
the six scientific stage commands and outputs remain unchanged. None of those
stage-output directories existed at recovery. The coupling controller waits on
this recovered comparison service, and its handoff checks now bind the new
comparison configuration hash and receipt location. Its fit and resampling
outputs were also absent. Both existing configuration checks passed, both
service handles were verified live, and the coupling state explicitly reports
`waiting_for_frame_producer`.

New plans are `metadata/recovery_20260925_rate_comparison_plan.json` and
`metadata/recovery_20260925_site_coupling_plan.json`. Exact commands and producer
identities are in `metadata/recovery_20260925_coupling_controller_launches.json`.
Original scientific scope and limits remain: 1,952 optimization diagnostics
before comparison and frame assembly, followed by 24 coupling specifications,
48,000 whole-marker bootstrap fits, and 2,928 leave-one-marker-out fits. These
are queued computations, not results. Geometry-tree benchmark/readbacks,
paired-fit resampling, fourth PMSF analysis, and retrieval remain to be reviewed.

## Geometry-to-tree benchmark and readback recovery

The full geometry-to-tree benchmark now waits for the recovered geometry and
paired-fit services, using their current PID/creation-time identities. The
geometry completion handoff retains the original plan hash and output receipt;
the fit recovery handoff uses the September 25 recovery plan and control path.
All dependency pins were checked before launch. The three benchmark-stage
output directories and the full path-readback output were absent.

The full path and rank readback services were then connected to the new
benchmark controller. All three services were verified live and their state
files report waiting for their respective predecessors. The rank readback uses
a fresh output directory ending `readback-20260925-v1`, preserving its earlier
waiting-state directory. Scientific stage commands, pair counts, and resource
limits were retained.

Plans are `metadata/recovery_20260925_geometry_tree_benchmark_plan.json`,
`metadata/recovery_20260925_tree_path_readback_plan.json`, and
`metadata/recovery_20260925_geometry_rank_readback_plan.json`; process identities
and exact commands are in
`metadata/recovery_20260925_geometry_downstream_launches.json`. These queued
checks cover the full 2,527,033-pair benchmark and four tree paths per pair;
they do not represent completed validation or evolutionary results.
Paired-fit resampling, fourth PMSF analysis, and retrieval still need recovery.

## Remaining pre-reboot services restored

The paired-fit resampling controller and fourth crossed PMSF controller now
wait on the recovered producer identities. Original scientific scope, stage
paths, and resource limits are retained; new controller directories preserve
earlier records. Both services were verified live. The fourth PMSF calculation
remains serially gated behind the running third calculation. Plans are
`metadata/recovery_20260925_paired_resampling_plan.json` and
`metadata/recovery_20260925_crossed_pmsf_config.json`; process identities are in
`metadata/recovery_20260925_resampling_pmsf_launches.json`.

Retrieval recovery exposed an incomplete log tail consisting of exactly 1,869
zero bytes. The complete original file was copied to
`results/recovery-20260925/retrieval-log-original.jsonl` and hashed before repair.
All 1,430,818 complete preceding JSONL records parsed successfully. Only the
zero-filled tail was truncated, and the resulting full file hash was verified
against the parsed prefix. The readback preserves both hashes and backup path
in `metadata/recovery_20260925_retrieval_log_readback.json`. Latest accession
statuses include 1,424,630 verified records, 13 no-exact-full-length outcomes,
and one error; these are log statuses, not independently revalidated coordinates
or confidence-qualified coverage.

The unchanged retrieval runner was restored using
`metadata/recovery_20260925_structure_retrieval_plan.json`, with two CPU
equivalents, two HTTP workers, 64 GiB memory, and the prior disk guards. It
rechecks cached coordinate hashes before scheduling missing records. Its launch
is recorded in `metadata/recovery_20260925_structure_retrieval_launch.json`.
All previously listed pre-reboot services now have replacement services;
ongoing liveness, checkpoint validation, scientific audits, and the full project
deliverables remain unfinished. GPU prediction remains paused.

## Initial live checkpoint readback

All 14 replacement services were observed live after recovery, including waiting
controllers. The geometry worker produced durable checkpoints for markers
115730at2759 and 129234at2759. Independent enumeration from the original input
FASTA labels verified all 24,090 and 25,425 unordered taxon pairs respectively,
with no duplicates or omissions; payload hashes and configuration bindings also
matched. The records contain 23,900/190 and 25,163/262 accepted/excluded pairs.
Exact checkpoint hashes and scope are archived in
`metadata/recovery_20260925_geometry_first_checkpoint_readback.json`.
This validates checkpoint integrity and pair coverage for those two markers;
the full numerical/character audit still runs after the full cohort completes.

## Updated marker gap cache screen

A new immutable retrieval-log snapshot at 2026-09-25 18:00:47 UTC contains
1,438,074 complete records. Screening the original 3,656 distinct missing marker
sequences now finds 30 cached model candidates for 30 sequences and 30
marker–taxon links. The residual cache-unmatched gap is 3,626 distinct sequences
and 3,639 links. The previous September 23 screen had no candidates.

The existing producer checked sequence-hash and length metadata and coordinate
file hashes. An independent reverse traversal of every frozen record reproduced
the candidates and every gap count using the latest record per accession.
The plan, producer receipt, and readback are archived as
`metadata/current_marker_gap_cache_refresh_plan_20260925.json`,
`metadata/current_marker_gap_cache_refresh_receipt_20260925.json`, and
`metadata/current_marker_gap_cache_refresh_readback_20260925.json`. Large snapshot
and candidate files remain outside Git in
`results/structures/current-marker-gap-cache-refresh-20260925-v1`.

These are cached candidates, not newly confidence-qualified analysis entries.
The frozen catalogs and running analyses are unchanged. Coordinate-level sequence
validation, confidence qualification, and integration of the new candidates
remain to be completed. The residual gap does not establish absence from public
databases or count whole-proteome or ancestral prediction needs. GPU prediction
remains paused.

The 30 recovered candidates subsequently passed an independent coordinate
readback using `scripts/validate_recovered_marker_coordinates.py`. All 10,763
residues have complete, unambiguous CA coverage; coordinate-derived full-sequence
hashes match the expected sequences, all atom coordinates are finite, and
recomputed CA confidence summaries match the retrieval records. The per-model
results and provenance are in
`metadata/recovered_marker_coordinate_readback_20260925.json`.
PAE qualification, structural-alphabet encoding, and analysis integration remain
outstanding; successful coordinate validation alone does not complete those steps.

Version-matched PAE retrieval for all 30 recovered candidates is now complete,
with no failures. The matrices contain 4,753,517 entries (longest protein 943
residues); the existing two-worker CPU/HTTP retriever stored 1,735,511 compressed
bytes. A separate standard-library traversal checked every entry for finite,
nonnegative values within the declared maximum, square dimensions, exact
model/version and sequence metadata, and compressed/uncompressed checksums.
This validates retrieval, not the six-residue structural-alphabet context mask.

Reproduction commands from the repository root:

```bash
python scripts/prepare_recovered_marker_provenance.py --screen results/structures/current-marker-gap-cache-refresh-20260925-v1 --coordinate-readback metadata/recovered_marker_coordinate_readback_20260925.json --output results/structures/recovered-marker-provenance-20260925-v1
python scripts/retrieve_marker_pae.py --snapshot results/structures/recovered-marker-provenance-20260925-v1 --output results/structural_pae/recovered-markers-20260925-v1
```

These commands require new output directories when rerun; existing outputs are
immutable. The provenance-only inventory is explicitly not a marker mapping,
even though the generic retriever uses `mapping_snapshot` as its receipt field.
Receipts and readback are archived in `metadata/recovered_marker_provenance_receipt_20260925.json`,
`metadata/recovered_marker_pae_receipt_20260925.json`, and
`metadata/recovered_marker_pae_readback_20260925.json`. Encoding, context confidence
qualification, and integration remain outstanding.

All 30 candidates now have native Foldseek encodings, coordinate-derived feature
audits, and pLDDT/PAE qualification. The executable and all four downloaded native
source hashes match the previously reviewed build. Of 10,763 residues, 10,703
have valid native states; 60 invalid terminal states remain excluded. The
six-residue feature context retains 8,674 states at pLDDT >=70 and 8,669 at both
pLDDT >=70 and maximum directional context PAE <=10. A separate full readback
recomputed all 36 ordered PAE pairs for every valid context and confirmed the
arrays and counts. This does not establish biological accuracy.

Commands used after the provenance and PAE steps (same snapshot for each):

```bash
python scripts/extract_structural_alphabet.py --snapshot results/structures/recovered-marker-provenance-20260925-v1 --output results/structural_alphabet/native-recovered-markers-20260925-v1
python scripts/audit_native_exports.py --native results/structural_alphabet/native-recovered-markers-20260925-v1 --snapshot results/structures/recovered-marker-provenance-20260925-v1 --output metadata/recovered_marker_native_exports_20260925.json
python scripts/audit_3di_features.py --native results/structural_alphabet/native-recovered-markers-20260925-v1 --snapshot results/structures/recovered-marker-provenance-20260925-v1 --output results/structural_alphabet/coordinate-recovered-markers-20260925-v1
python scripts/qualify_native_pae.py --coordinates results/structural_alphabet/coordinate-recovered-markers-20260925-v1 --pae results/structural_pae/recovered-markers-20260925-v1 --output results/structural_alphabet/qualified-recovered-markers-20260925-v1
python scripts/readback_afdb_pae_qualification.py --snapshot results/structures/recovered-marker-provenance-20260925-v1 --coordinates results/structural_alphabet/coordinate-recovered-markers-20260925-v1 --qualified results/structural_alphabet/qualified-recovered-markers-20260925-v1 --pae results/structural_pae/recovered-markers-20260925-v1 --output metadata/recovered_marker_qualification_readback_20260925.json
```

The extraction used the reviewed Foldseek binary from `SOFTWARE/foldseek/bin`
on PATH; numerical workers used OPENBLAS_NUM_THREADS=1 and OMP_NUM_THREADS=1.
Model-level encoding is complete, but marker alignment/residue mapping and
integration into a new analysis snapshot remain outstanding. Existing running
analyses retain their frozen inputs.

The unchanged marker-mapping script mapped all 30 recovered models to 30
previously missing marker–taxon entries, spanning 30 markers in taxon F1080349.
It produced 8,256 matrix-to-protein residue links. All 30 link identities were
checked against the original gap table. This is a supplemental mapping snapshot;
its one-taxon scope cannot support a separate phylogenetic fit. Independent
residue-position readback and merging into a new analysis snapshot remain.

The mapping receipt is archived in
`metadata/recovered_marker_mapping_receipt_20260925.json`. Input inventory
provenance is in `metadata/recovered_marker_inventory_receipt_20260925.json`.
The adapter emits one JSONL record per candidate model, with status `verified`,
`record_id` equal to `recovered:{model_id}:v{version}`, the unchanged model in a
single-element `models` list, and the candidate's UniProt accession list under
`source_uniprot_accessions`. It does not invent UniProt accessions.

```bash
python scripts/map_marker_structures.py --inventory results/structures/recovered-marker-inventory-20260925-v1/inventory.jsonl --output results/structural_markers/recovered-markers-20260925-v1
```

Independent residue-position readback is now complete for all 30 supplemental
marker links and all 8,256 residue links. The readback uses alignment-prefix
counts instead of the producer's precomputed position list, checks every retained
matrix column (including gaps), reconstructs coordinate sequences with
MMCIFParser, and verifies CA confidence and per-link summaries. Source hashes
are checked again after the audit and preserved in
`metadata/recovered_marker_mapping_readback_20260925.json`.

```bash
python scripts/readback_recovered_marker_mapping.py --mapping results/structural_markers/recovered-markers-20260925-v1 --output metadata/recovered_marker_mapping_readback_20260925.json
```

This completes the supplemental mapping readback. Binding the qualified encodings
to this mapping and integrating them with the larger cohort remain separate work.

Confidence qualification is now bound to the final supplemental marker mapping.
The six existing extraction/audit/retrieval/qualification/readback commands were
rerun on the small 30-model cohort against that mapping; PAE retrieval reused
verified local caches. Commands, input pins, and resource estimates are recorded
in `metadata/recovered_mapped_qualification_plan_20260925.json`; per-stage logs
and completion receipt are in
`results/structural_alphabet/recovered-mapped-controller-20260925-v1`.
All arrays in all 30 resulting encodings equal their previously validated arrays
(including missing-value positions), and the full independent PAE readback again
retains 8,669 states. Archived metadata use the `recovered_mapped_` prefix.
The mapping-bound encoding snapshot is
`results/structural_alphabet/qualified-recovered-mapped-20260925-v1`.
The broader cohort union and paired input preparation remain outstanding.

The recovered supplement passes an identity preflight for a disjoint union with
`gdm-current-mapping-20260922-v1`: no shared model IDs/versions, full-sequence
hashes, or marker–taxon entries; both cohorts use the same alignment matrix and
recorded GDM AlphaFold Monomer v2.0 pipeline. Both qualified encoding receipts
bind to their respective mappings. A union would contain 30,618 models, 31,414
marker links, and 11,202,520 residue links. These are expected union dimensions,
not a completed merged dataset. Evidence and source receipt hashes are in
`metadata/recovered_afdb_union_preflight_20260925.json`.
The existing local-inventory merger is ESMFold-specific and cannot be applied
unchanged to these AFDB records. A provenance-preserving AFDB table union remains
the next integration step; matching provider/tool labels do not establish
identical undisclosed prediction settings.

The AFDB union is now built and independently read back in full. It contains
30,618 models, 31,414 marker–taxon entries, and 11,202,520 residue links in
`results/structural_markers/afdb-recovered-union-20260925-v1/mapping`, with
qualified encoding references in the sibling `encodings` directory. The merger
checks disjoint identities, source receipt bindings, unchanged encoding hashes,
complete per-link residue counts, and ordered unique matrix columns. Source
model metadata and qualification receipts remain attached to each cohort.

The separate readback compared every model provenance record, marker-link row,
and encoding-summary row to the source union, and all 11,202,520 decompressed
residue rows byte-for-byte to the original cohorts. It passed. This is a derived
integration, not a new coordinate audit or evolutionary result. The combined
encodings contain 10,836,903 valid states passing the six-residue pLDDT/PAE mask;
paired alignment coverage will differ after matrix projection and taxon filters.

```bash
python scripts/merge_disjoint_afdb_mappings.py --plan metadata/recovered_afdb_union_plan_20260925.json
python scripts/readback_disjoint_afdb_union.py --plan metadata/recovered_afdb_union_plan_20260925.json --output metadata/recovered_afdb_union_readback_20260925.json
```

The immutable plan, mapping/encoding receipts, and readback are archived under
`metadata/recovered_afdb_union_*_20260925.json`. Existing running fits retain
their original inputs. New paired-input preparation and its independent readback
remain outstanding. GPU prediction remains paused.

Paired-input preparation for the union has started under the transient user unit
`fungal-recovered-afdb-paired-inputs-20260925.service`: one CPU equivalent,
32 GiB memory, no swap, no GPU. The plan reserves 10 GiB output with a 100 GiB
free-disk guard; runtime is uncalibrated (0.5–24 hours planning range inherited
from the prior full-cohort stage). Source hashes and commands are bound through
`metadata/recovered_afdb_paired_inputs_plan_20260925.json`; the observed process
identity is in `metadata/recovered_afdb_paired_inputs_launch_20260925.json`.

```bash
python scripts/advance_afdb_union_paired_inputs.py --plan metadata/recovered_afdb_paired_inputs_plan_20260925.json
```

The controller runs unchanged full paired-input preparation followed by the
independent reconstruction of every character from qualified arrays, and checks
that filtering and matrix identity match the existing AlphaFold baseline. Logs
and state are in `results/phylogeny/paired-inputs-afdb-recovered-controller-20260925-v1`.
Preparation/readback are running, not completed; no new phylogenetic fits have
been launched for the expanded input snapshot.

Expanded paired preparation and the complete independent character readback have
finished successfully: 125 markers, 339 represented taxa, 31,134 eligible
marker–taxon entries, 47,529 retained marker columns, and 9,453,757 observed
paired cells. The audit examined all 526-by-125 coverage cells and reconstructed
every emitted character and observation mask from the qualified arrays.
Controller, preparation, and readback receipts are archived as
`metadata/recovered_afdb_paired_controller_receipt_20260925.json`,
`metadata/recovered_afdb_paired_inputs_receipt_20260925.json`, and
`metadata/recovered_afdb_paired_inputs_readback_20260925.json`.

A separate comparison against the original AlphaFold paired snapshot checked
all 18,892,284 previously observed AA plus 3Di characters at their original
matrix positions; none changed. The 30 supplemental entries in F1080349 all
pass eligibility, adding 7,615 observed paired cells. Ninety-five marker
alignments are unchanged; 30 differ by the newly included taxon entry.

```bash
python scripts/compare_expanded_paired_inputs.py --previous results/phylogeny/paired-inputs-gdm-current-20260923-v2 --expanded results/phylogeny/paired-inputs-afdb-recovered-20260925-v1 --output metadata/recovered_afdb_paired_input_changes_20260925.json
```

The existing AlphaFold fits continue against their original inputs. Updated
inference must refit the 30 changed markers; reuse of any unchanged-marker fit
requires checking its exact inputs, model settings, and completion provenance.
No expanded-cohort fits are claimed complete by these input audits.

Updated fitting has started for the 30 changed markers only, using an explicitly
labeled subset of the full expanded paired snapshot. The selection script
checks the full parent readback, unchanged filtering, and actual AA/3Di/column
file hashes. Every selected file was also independently compared with its
expanded parent before launch. The other 95 markers remain in the full cohort;
this subset does not redefine the project scope.

The unchanged fit runner uses AA LG+F+G4 topology searches with 1,000 SH-aLRT
and 1,000 ultrafast bootstrap replicates, then AF+G4, AF+F+G4, and LLM+G4
structural fits on each fixed AA topology: 120 fits in total. Four single-thread
workers are live under `fungal-recovered-afdb-changed-fits-20260925.service`,
with four CPU equivalents, 16 GiB total memory, no swap, and no GPU. The recorded
planning allowance is 20 GiB output and 1–14 days wall time (uncalibrated, not an
ETA). The unit and native worker identities are archived in
`metadata/recovered_afdb_changed_fits_launch_20260925.json` and commands/settings
in `metadata/recovered_afdb_changed_fits_plan_20260925.json`.

```bash
python scripts/select_changed_paired_markers.py --previous results/phylogeny/paired-inputs-gdm-current-20260923-v2 --expanded results/phylogeny/paired-inputs-afdb-recovered-20260925-v1 --readback results/phylogeny/paired-inputs-afdb-recovered-20260925-v1-readback.json --output results/phylogeny/paired-inputs-afdb-recovered-changed-20260925-v1
```

Fit outputs are in `results/phylogeny/paired-fits-afdb-recovered-changed-20260925-v1`.
Original full-cohort fits continue. Subsequent assembly of expanded-cohort fit
results must validate exact input/model/settings provenance for reuse of the 95
unchanged markers and fully audit the 30 new fits. No updated branch results are
claimed complete at launch.

### Geometry checkpoint readback at 18:55 UTC

Independent payload-hash and complete taxon-pair-grid checks passed for all 20 completed marker checkpoints: 387,456 accepted pairs and 5,012 excluded pairs. Evidence: `metadata/recovery_20260925_geometry_twenty_checkpoint_readback.json`. The earlier status count of 21 JSON files included `config.json`; the correct completed-marker count is 20 of 122. This readback verifies checkpoint integrity and pair coverage, not numerical geometry. Full numerical validation remains pending the completed cohort. Live geometry worker PID 443274 continues running.

### Residual marker prediction gap composition

`scripts/summarize_refreshed_marker_gaps.py` joins checksum-bound original gap tables to the September 25 cache refresh. Reproduce with `python scripts/summarize_refreshed_marker_gaps.py --gaps results/structures/current-marker-gaps-20260922-v1 --refresh results/structures/current-marker-gap-cache-refresh-20260925-v1 --output NEW_OUTPUT.json`. The archived result is `metadata/current_marker_residual_gap_summary_20260925.json`.

The remaining 3,626 unique sequences represent 3,639 marker records across 508 taxa and 118 markers. Disjoint categories are: 3,512 canonical sequences above 1,024 residues; 21 noncanonical sequences above 1,024; four canonical sequences at most 1,024; and 89 noncanonical sequences at most 1,024. The maximum length is 5,502 residues. A separate Bio.SeqIO pass over all original unaligned marker FASTAs independently reproduced the sequence universe, 3,639 links, and all four category counts using sequence SHA-256 and direct alphabet/length checks.

The remaining set is dominated by long proteins, so extrapolating throughput from the previous short-protein queue is inappropriate. Length alone does not establish GPU feasibility; noncanonical sequences need explicit handling before inference. This characterization launches no predictions and does not imply that the remaining models are absent from public databases.

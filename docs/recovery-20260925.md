# Recovery after the September 25 host reboot

The host had approximately eight minutes uptime at the first recovery check.
All four previously monitored project process handles were missing, and no
fungal transient user services remained loaded. This was confirmed termination,
not an observation timeout. An unrelated IQ-TREE process was left untouched.

Three CPU services were restored with new boot-bound recovery plans, preserving
the original plans and outputs:

- Species PMSF: `metadata/recovery_20260925_pmsf_plan.json`, 16 CPU equivalents,
  650 GiB memory limit. The checkpoint was hashed and copied before recovery;
  the native log confirms restoration of site frequencies, the initial tree,
  and 1,000 bootstrap trees. This does not mean inference is complete.
- ESMFold-based paired fits: `metadata/recovery_20260925_paired_fit_plan.json`,
  four CPU equivalents, 16 GiB memory limit; 228 existing fit receipts.
- AlphaFold-based paired fits:
  `metadata/recovery_20260925_afdb_paired_fit_plan.json`, four CPU equivalents,
  16 GiB memory limit; 120 existing fit receipts.

Commands, process identities, and boot ID are recorded in
`metadata/recovery_20260925_core_launches.json`. All three services were verified
live with native child processes. Original dependency pins are checked by the
recovery wrappers. Runtime remaining is uncalibrated. No paid resources or GPU
predictions were started.

The geometry log contains 101 processed markers, but inspection of
`prepare_paired_site_geometry.py` showed that results were accumulated in memory
until the entire cohort finished. The final output directory does not exist.
Those log entries therefore do not constitute recoverable geometry outputs.
Geometry requires checkpointing before recomputation. Its dependent controllers,
the other queued analyses, and retrieval still require recovery review; they
must not be described as running based on their pre-reboot state files.

## Geometry recovery

A separate `prepare_checkpointed_paired_site_geometry.py` preserves the original
geometry calculations and output format while saving each completed marker.
Checkpoint writes use a temporary file, fsync, atomic rename, and directory
fsync. An exclusive lock prevents concurrent writers. Resume checks source
receipt hashes, implementation hashes, marker identities, pair counts, and
serialized payload hashes before reusing marker results. Incomplete temporary
writes are not used. The original producer remains unchanged so previous
provenance pins remain valid.

`check_geometry_checkpoint_recovery.py` compared original and checkpointed
outputs for two real taxa from one marker, then repeated the new calculation
from its checkpoint and deliberately corrupted the payload hash. Both TSV
comparisons were byte-identical, and corruption was rejected. Only cohort
receipt validation was mocked to limit this test; coordinate and PAE hash checks
were executed. This is a recovery regression test, not a full numerical audit.

The complete 122-marker, 2,527,033-pair calculation was relaunched with one CPU
equivalent, 64 GiB memory, no swap, and no GPU through
`scripts/recover_completed_geometry.py`. It reuses the completed PAE union and
will run the original full grid/character audit after geometry finishes. The
recovery plan and launch identity are
`metadata/recovery_20260925_geometry_plan.json` and
`metadata/recovery_20260925_geometry_launch.json`. Checkpoints reside in
`results/structural_comparisons/paired-site-esmfold-checkpoints-20260925-v1/`.
Prior in-memory progress cannot be recovered and must be recomputed.
Dependent-controller recovery remains outstanding.

## Site-rate and exposure controller recovery

The site-rate export and site parsimony/exposure controllers have been restored
with the recovered paired-fit producer's exact PID, creation time, and command.
Both passed their existing `--check-config` checks, including all dependency
pins. All six stage-output directories were absent before launch. The original
stage commands, output locations, prerequisite completion checks, and resource
limits are unchanged; fresh controller directories preserve the prior records.

Plans are `metadata/recovery_20260925_site_rate_exports_plan.json` and
`metadata/recovery_20260925_site_exposure_plan.json`; commands and process
identities are in `metadata/recovery_20260925_site_controller_launches.json`.
Both live services logged that they are waiting for producer 75123. They will
validate the completed fit/audit receipts before beginning their stages.
These are queued analyses, not completed exports or exposure results.
The downstream rate-comparison/coupling controllers, geometry-tree benchmark
and readbacks, resampling, fourth PMSF analysis, and retrieval still need review.

## Rate-comparison and coupling controller recovery

The rate-comparison controller now waits on the recovered rate-export and
exposure services. Its prerequisite paths point to the new controller receipts;
the six scientific stage commands and outputs remain unchanged. None of those
stage-output directories existed at recovery. The coupling controller waits on
this recovered comparison service, and its handoff checks now bind the new
comparison configuration hash and receipt location. Its fit and resampling
outputs were also absent. Both existing configuration checks passed, both
service handles were verified live, and the coupling state explicitly reports
`waiting_for_frame_producer`.

New plans are `metadata/recovery_20260925_rate_comparison_plan.json` and
`metadata/recovery_20260925_site_coupling_plan.json`. Exact commands and producer
identities are in `metadata/recovery_20260925_coupling_controller_launches.json`.
Original scientific scope and limits remain: 1,952 optimization diagnostics
before comparison and frame assembly, followed by 24 coupling specifications,
48,000 whole-marker bootstrap fits, and 2,928 leave-one-marker-out fits. These
are queued computations, not results. Geometry-tree benchmark/readbacks,
paired-fit resampling, fourth PMSF analysis, and retrieval remain to be reviewed.

## Geometry-to-tree benchmark and readback recovery

The full geometry-to-tree benchmark now waits for the recovered geometry and
paired-fit services, using their current PID/creation-time identities. The
geometry completion handoff retains the original plan hash and output receipt;
the fit recovery handoff uses the September 25 recovery plan and control path.
All dependency pins were checked before launch. The three benchmark-stage
output directories and the full path-readback output were absent.

The full path and rank readback services were then connected to the new
benchmark controller. All three services were verified live and their state
files report waiting for their respective predecessors. The rank readback uses
a fresh output directory ending `readback-20260925-v1`, preserving its earlier
waiting-state directory. Scientific stage commands, pair counts, and resource
limits were retained.

Plans are `metadata/recovery_20260925_geometry_tree_benchmark_plan.json`,
`metadata/recovery_20260925_tree_path_readback_plan.json`, and
`metadata/recovery_20260925_geometry_rank_readback_plan.json`; process identities
and exact commands are in
`metadata/recovery_20260925_geometry_downstream_launches.json`. These queued
checks cover the full 2,527,033-pair benchmark and four tree paths per pair;
they do not represent completed validation or evolutionary results.
Paired-fit resampling, fourth PMSF analysis, and retrieval still need recovery.

## Remaining pre-reboot services restored

The paired-fit resampling controller and fourth crossed PMSF controller now
wait on the recovered producer identities. Original scientific scope, stage
paths, and resource limits are retained; new controller directories preserve
earlier records. Both services were verified live. The fourth PMSF calculation
remains serially gated behind the running third calculation. Plans are
`metadata/recovery_20260925_paired_resampling_plan.json` and
`metadata/recovery_20260925_crossed_pmsf_config.json`; process identities are in
`metadata/recovery_20260925_resampling_pmsf_launches.json`.

Retrieval recovery exposed an incomplete log tail consisting of exactly 1,869
zero bytes. The complete original file was copied to
`results/recovery-20260925/retrieval-log-original.jsonl` and hashed before repair.
All 1,430,818 complete preceding JSONL records parsed successfully. Only the
zero-filled tail was truncated, and the resulting full file hash was verified
against the parsed prefix. The readback preserves both hashes and backup path
in `metadata/recovery_20260925_retrieval_log_readback.json`. Latest accession
statuses include 1,424,630 verified records, 13 no-exact-full-length outcomes,
and one error; these are log statuses, not independently revalidated coordinates
or confidence-qualified coverage.

The unchanged retrieval runner was restored using
`metadata/recovery_20260925_structure_retrieval_plan.json`, with two CPU
equivalents, two HTTP workers, 64 GiB memory, and the prior disk guards. It
rechecks cached coordinate hashes before scheduling missing records. Its launch
is recorded in `metadata/recovery_20260925_structure_retrieval_launch.json`.
All previously listed pre-reboot services now have replacement services;
ongoing liveness, checkpoint validation, scientific audits, and the full project
deliverables remain unfinished. GPU prediction remains paused.

## Initial live checkpoint readback

All 14 replacement services were observed live after recovery, including waiting
controllers. The geometry worker produced durable checkpoints for markers
115730at2759 and 129234at2759. Independent enumeration from the original input
FASTA labels verified all 24,090 and 25,425 unordered taxon pairs respectively,
with no duplicates or omissions; payload hashes and configuration bindings also
matched. The records contain 23,900/190 and 25,163/262 accepted/excluded pairs.
Exact checkpoint hashes and scope are archived in
`metadata/recovery_20260925_geometry_first_checkpoint_readback.json`.
This validates checkpoint integrity and pair coverage for those two markers;
the full numerical/character audit still runs after the full cohort completes.

## Updated marker gap cache screen

A new immutable retrieval-log snapshot at 2026-09-25 18:00:47 UTC contains
1,438,074 complete records. Screening the original 3,656 distinct missing marker
sequences now finds 30 cached model candidates for 30 sequences and 30
marker–taxon links. The residual cache-unmatched gap is 3,626 distinct sequences
and 3,639 links. The previous September 23 screen had no candidates.

The existing producer checked sequence-hash and length metadata and coordinate
file hashes. An independent reverse traversal of every frozen record reproduced
the candidates and every gap count using the latest record per accession.
The plan, producer receipt, and readback are archived as
`metadata/current_marker_gap_cache_refresh_plan_20260925.json`,
`metadata/current_marker_gap_cache_refresh_receipt_20260925.json`, and
`metadata/current_marker_gap_cache_refresh_readback_20260925.json`. Large snapshot
and candidate files remain outside Git in
`results/structures/current-marker-gap-cache-refresh-20260925-v1`.

These are cached candidates, not newly confidence-qualified analysis entries.
The frozen catalogs and running analyses are unchanged. Coordinate-level sequence
validation, confidence qualification, and integration of the new candidates
remain to be completed. The residual gap does not establish absence from public
databases or count whole-proteome or ancestral prediction needs. GPU prediction
remains paused.

The 30 recovered candidates subsequently passed an independent coordinate
readback using `scripts/validate_recovered_marker_coordinates.py`. All 10,763
residues have complete, unambiguous CA coverage; coordinate-derived full-sequence
hashes match the expected sequences, all atom coordinates are finite, and
recomputed CA confidence summaries match the retrieval records. The per-model
results and provenance are in
`metadata/recovered_marker_coordinate_readback_20260925.json`.
PAE qualification, structural-alphabet encoding, and analysis integration remain
outstanding; successful coordinate validation alone does not complete those steps.

# September 26 continuation

Command access was restored after the local sandbox failed before execution. No analysis was restarted based solely on the observation failure. Live processes, systemd terminal states, logs and receipts were inspected first.

The ESMFold paired-fit producer completed 488 fits across 122 markers and its audit/controller completed. Site-rate exports and site-parsimony/exposure controllers also report completion. The recovered AlphaFold producer completed 120 fits across 30 changed markers; its separate full fit audit remains outstanding. The original AlphaFold producer remains active (404/500 fit receipts at the initial check), as do the species-tree search and structure retrieval. GPU predictions were not launched.

All 122 geometry checkpoints completed. The geometry audit reports 2,527,033 pairs (2,475,703 accepted; 51,330 excluded), complete pair-grid/character checks, and independent numerical checks on one pair per marker. This is not numerical validation of every pair.

The old geometry recovery controller failed after the audit succeeded: it passed the audit's self-contained receipt to a generic artifact-manifest reader, causing `KeyError: artifacts`. The dependent benchmark and two readbacks consequently failed without producing their analytical outputs.

`scripts/recover_completed_geometry_receipt.py` preserves the old recovery script and handles the readback receipt explicitly: it checks all four source receipt hashes and their artifact manifests, the audit script hash, total pair counts, and the number of distinct sampled markers. It retains the original plan and all original input pins. A new recovery plan and control directory preserve the failed attempt. The corrected controller completed successfully using existing results; it did not rerun geometry or its audit.

```bash
python scripts/recover_completed_geometry_receipt.py --plan metadata/recovery_20260926_geometry_plan.json
```

The command above was executed once; the completion receipts use exclusive creation and must not be overwritten by a rerun. The archived receipt is `metadata/recovery_20260926_geometry_completed_receipt.json`.

A new benchmark plan points to a fresh control directory and pins the repaired geometry completion. Its analytical output paths were checked absent before launch. The original resource allowances are retained (one CPU, 64 GiB RAM, no swap/GPU). Two new readback plans bind the new benchmark PID and creation time, with fresh control/output directories and their original memory limits. Launch commands and process identities are recorded in the September 26 launch metadata. The old failed services and logs are retained. Completion of these newly launched downstream stages remains outstanding.

## Recovered AlphaFold fit audit

The separate audit for the 30 changed markers now passes: 120 fits and 15,450 matched branches. It checks input/model provenance and artifacts, paired observation masks, fixed topology identity, report model identity, parsed/report total lengths and branch-table values. All 120 fits contain near-zero branches and recorded warnings; none has a branch length at least 10. The audit retains all 221 warning rows. These point estimates do not establish acceleration, confidence intervals or biological validity. A checksum-bound warning-category inventory is archived separately; categories overlap and do not replace the complete warnings table.

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python scripts/summarize_paired_marker_fits.py --inputs results/phylogeny/paired-inputs-afdb-recovered-changed-20260925-v1 --models data/structural_models/garg-hochberg-v3 --fits results/phylogeny/paired-fits-afdb-recovered-changed-20260925-v1 --output results/phylogeny/paired-fit-audit-afdb-recovered-changed-20260926-v1
```

The immutable audit output was created successfully. Its artifact checksums were subsequently read back. Receipt: `metadata/recovered_afdb_changed_fit_audit_20260926.json`; warning inventory: `metadata/recovered_afdb_changed_fit_warnings_20260926.json`. The original 125-marker AlphaFold batch is still running (404/500 fits at this check); reuse of its 95 unchanged markers and provenance-preserving assembly with these 30 refits remains outstanding.

The restarted tree-path/geometry benchmark has now completed its paths, ranks and figure stages. All three stage-receipt checksums and all declared artifacts were independently rehashed successfully. The completion receipt is archived as `metadata/recovery_20260926_geometry_benchmark_completed_receipt.json`. The generated PNG was visually inspected: panels, model labels, marker connections and descriptive/uncertainty captions render legibly without clipping. Geometry uses the shared paired sites specified by the source pipeline; the whole-protein RMSD panel must not be interpreted as coverage of unobserved residues. The full path and rank readback processes started automatically and were confirmed running. Their completion is still outstanding, as are phylogenetic and uncertainty-adjusted inference. Resampling and rate-optimization workers were also confirmed live; no restart was needed.

The independent rank readback has passed for all 122 markers: 2,475,703 accepted pairs, all 1,464 correlation rows and all 12 equal-marker summary rows. Maximum absolute correlation difference was 2.22e-16. The readback recomputes the complete rank grid, denominators, review labels and equal-marker summaries using SciPy Spearman rather than the producer's rankdata/corrcoef call; it shares numerical/ranking libraries and does not establish phylogenetically independent significance. Receipt archived in `metadata/recovery_20260926_geometry_rank_completed_readback.json`. Full tree-path readback remains running at this checkpoint.

The full tree-path readback and its controller have now completed successfully. Across 122 markers, the auditor checked all 2,527,033 pair identities (2,475,703 accepted; 51,330 excluded) and all 10,108,132 tree-path values using adjacency-graph traversal. Maximum absolute path difference was 5.3291e-15. Inherited geometry strings, review/uncertainty labels and summaries also passed. All 1,354 recorded source hashes and the controller-to-audit receipt binding were independently rehashed after completion. Archived receipts: `metadata/recovery_20260926_tree_path_completed_readback.json` and `metadata/recovery_20260926_tree_path_completed_controller.json`. The audit uses the same fitted trees and BioPython parser; it is not independent phylogenetic inference or numerical recomputation of every geometry value. Both full path and rank readbacks are now complete; branch uncertainty, prediction effects and biological inference remain outstanding.

## Expanded AlphaFold fit source compatibility

A source inventory now establishes which original fit directory belongs to each
of the 125 expanded markers: 95 unchanged markers use the original run and 30
changed markers use the supplemental run. Both runs have identical settings
apart from cohort input hashes and dimensions; model, executable and producer
hashes agree. All selected AA alignments, 3Di alignments and column maps match
the expanded inputs exactly. An independent byte comparison checked all 375
files, all inventory source pins and the exact 30-marker change set against the
previous input-change audit.

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python scripts/prepare_paired_fit_source_inventory.py --previous results/phylogeny/paired-inputs-gdm-current-20260923-v2 --expanded results/phylogeny/paired-inputs-afdb-recovered-20260925-v1 --changed results/phylogeny/paired-inputs-afdb-recovered-changed-20260925-v1 --previous-fits results/phylogeny/paired-marker-fits-gdm-current-20260923-v2 --changed-fits results/phylogeny/paired-fits-afdb-recovered-changed-20260925-v1 --models data/structural_models/garg-hochberg-v3 --output metadata/recovered_afdb_fit_source_inventory_20260926.json
```

The immutable inventory was created successfully. It does not claim fit
completion, copy or relabel fit artifacts, or replace either source audit.
The original run still needs to finish and pass its full audit. Subsequent
integration must recheck the pinned inputs/settings and both source audits,
retain original fit/configuration paths, and select each marker once using
this inventory. A combined result and downstream uncertainty remain pending.

## Prepared source-preserving table integration

`scripts/combine_audited_paired_fit_sources.py` now implements the pending
integration. It requires completed source audits, checks their run bindings,
revalidates selected fit artifacts/configurations/topology sources and expanded
input hashes, and compares native branch rows with the audit rows. It appends
source run, fit directory and audit directory columns to the selected fit,
branch and warning tables. It produces a distinct table-collection receipt;
it must not be passed to consumers expecting a native single-run fit directory.

The following command is prepared, **not completed**; the original audit is
still absent:

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python scripts/combine_audited_paired_fit_sources.py --inventory metadata/recovered_afdb_fit_source_inventory_20260926.json --expanded results/phylogeny/paired-inputs-afdb-recovered-20260925-v1 --previous-audit results/phylogeny/paired-marker-fit-audit-gdm-current-20260923-v2 --changed-audit results/phylogeny/paired-fit-audit-afdb-recovered-changed-20260926-v1 --output results/phylogeny/paired-fit-tables-afdb-recovered-20260926-v1
```

Validation used a temporary output location: the real missing source audit and
an altered source checksum were both rejected without creating output. Selection
checks on the completed supplemental audit preserved every original field and
added correct provenance for 60 fit rows, 7,889 branch rows and 106 warning rows
from alternating markers. Full successful integration and independent output
readback remain pending; these checks do not claim either has occurred.
